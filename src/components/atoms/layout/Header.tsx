import styled from "styled-components"

export const Header = () => {
    return (
        <SHeader>
          Header
        </SHeader>
    );
};

const SHeader = styled.header`
  background-color: #11999e;
  color: #fff;
  text-align: center;
  padding: 8px 0;
`
